<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.almarri.omar.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".InfoActivity"
            android:label="معلومات عامه"></activity>
        <activity
            android:name=".LocActivity"
            android:label="الموقع"></activity>
        <activity
            android:name=".PicActivity"
            android:label="معرض الصور"></activity>
        <activity
            android:name=".GrowActivity"
            android:label="التطور والازدهار"></activity>
        <activity
            android:name=".ViewImage"
            android:label="معرض الصور"></activity>
    </application>

</manifest>